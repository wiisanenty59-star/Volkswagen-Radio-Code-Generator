kind = "web"
previewPath = "/"
title = "VW Radio Code Generator"
version = "1.0.0"
id = "artifacts/vw-radio-code"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25952

[services.development]
run = "pnpm --filter @workspace/vw-radio-code run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/vw-radio-code", "run", "build" ]
publicDir = "artifacts/vw-radio-code/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25952"
BASE_PATH = "/"
